<h1>Create A New Category</h1>

<%= form_for(@category) do |f| %>
  <p><%= f.label :name, "name" %><br />
  <%= f.text_field :name, placeholder: "category name" %></p>
  <p><%= f.submit "Create Category" %></p>
<% end %>
